# Python Interview Refresher

One short file per topic. Skim the list before an interview; open the file to refresh.

## Topics

- [Lists vs Tuples](lists-and-tuples.md) — mutability, shallow immutability, hashability, when to use each
- [Variables, References & Copies](variables-and-copies.md) — `b = a` shares, shallow vs deep copy, `is` vs `==`
- [Function Arguments](function-arguments.md) — pass by object reference, mutable default argument trap

## To add

- Dict internals (ordering, hashing, `get`/`setdefault`/`defaultdict`)
- Comprehensions & generators (lazy eval, `yield`)
- `*args` / `**kwargs`, unpacking
- Decorators & closures
- Iterators vs iterables
- Exceptions (`else`/`finally`, custom exceptions)
- String immutability & f-strings
- `__eq__` / `__hash__` / dunder methods
- GIL, threads vs processes vs async
